# Verificação de acesso à Vercel

Em 27 de agosto de 2026, a conta Vercel atualmente conectada no navegador foi identificada como **apexhardfire-8689**. A busca por `hub-seja-meu-socio` não retornou nenhum projeto nesse time. Assim, não é seguro criar Neon, Blob ou variáveis de ambiente nesta conta, pois eles poderiam ser vinculados ao projeto errado.

Para finalizar a migração, o usuário precisa disponibilizar o projeto correto no navegador ou informar o time Vercel onde `hub-seja-meu-socio` está hospedado. Depois disso, devem ser conectados um banco Neon Postgres, um Blob Store e os segredos `DATABASE_URL`, `BLOB_READ_WRITE_TOKEN` e `JWT_SECRET`.
